type Cafeteria {
  id: ID!
  nombre: String!
  escuela: String!
  activa: Boolean!
}

type Usuario {
  id: ID!
  nombre: String!
  correo: String!
  cafeteriaId: ID!
}

type Producto {
  id: ID!
  nombre: String!
  precio: Float!
  disponible: Boolean!
  cafeteriaId: ID!
}

type ItemPedido {
  productoId: ID!
  nombreProducto: String!
  precioUnitario: Float!
  cantidad: Int!
}

type Pedido {
  id: ID!
  usuarioId: ID!
  cafeteriaId: ID!
  items: [ItemPedido!]!
  total: Float!
  estado: String!
}

# Reporte generado por el pipeline de agregacion, siempre acotado a una cafeteria
type ProductoMasVendido {
  nombreProducto: String!
  unidadesVendidas: Int!
  ingresosTotales: Float!
}

input ItemPedidoInput {
  productoId: ID!
  cantidad: Int!
}

type Query {
  cafeterias: [Cafeteria!]!
  cafeteria(id: ID!): Cafeteria

  productosPorCafeteria(cafeteriaId: ID!): [Producto!]!
  producto(id: ID!): Producto

  pedido(id: ID!): Pedido
  pedidosPorUsuario(usuarioId: ID!): [Pedido!]!
  pedidosPorCafeteria(cafeteriaId: ID!): [Pedido!]!

  productosMasVendidos(cafeteriaId: ID!): [ProductoMasVendido!]!
}

type Mutation {
  registrarCafeteria(nombre: String!, escuela: String!): Cafeteria!
  registrarUsuario(nombre: String!, correo: String!, cafeteriaId: ID!): Usuario!
  registrarProducto(nombre: String!, precio: Float!, disponible: Boolean, cafeteriaId: ID!): Producto!
  crearPedido(usuarioId: ID!, cafeteriaId: ID!, items: [ItemPedidoInput!]!): Pedido!
  actualizarEstadoPedido(pedidoId: ID!, nuevoEstado: String!): Pedido!
}